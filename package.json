{
  "name": "family-tree-app",
  "private": true,
  "description": "Monorepo root; Railway builds the backend from backend/",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "cd backend && pnpm install && pnpm exec prisma generate",
    "db:push": "cd backend && pnpm exec prisma db push",
    "start": "cd backend && pnpm exec prisma db push && pnpm run start"
  }
}
